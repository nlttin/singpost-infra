name: Standard CI/CD Pipeline

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment to deploy'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - sit
          - uat
          - preprod
          - prod

permissions:
  contents: read
  security-events: write
  actions: read

env:
  TF_VERSION: "1.14.0"
  TG_VERSION: "0.77.3"

jobs:
  # ==========================================
  # PARALLEL STAGE 1: FAST CHECKS
  # ==========================================
  checkout-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

  gitleaks-scan:
    needs: checkout-code
    uses: nlttin/singpost-cicd-components/.github/workflows/reusable-workflow-gitleaks.yml@dev
    with:
      scan_mode: pr
      fail-on-detection: false
    secrets: inherit

  checkov-scan:
    needs: checkout-code
    uses: nlttin/singpost-cicd-components/.github/workflows/reusable-workflow-checkov.yml@dev
    with:
      config-path: ./.github/config/checkov.yml
    secrets: inherit

  # ==========================================
  # STAGE 2: SECURITY SCANS
  # ==========================================
  security-scans:
    needs: [gitleaks-scan, checkov-scan]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mocking CodeQL & SonarQube"

  # ==========================================
  # STAGE 3: DEPLOY DEV
  # ==========================================
  deploy-dev:
    needs: security-scans
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/dev') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'dev')
    runs-on: ubuntu-latest
    environment: development
    permissions:
      contents: read
      id-token: write
    env:
      TG_DIR: live/dev/gcp
      GKE_CLUSTER: sp-logistic-dev-gke
      APIGEE_ENV: sp-logistic-apigee-env-dev
      APIGEE_PROXY: sp-logistic-dev-gke-proxy
      REGION: asia-southeast1
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SA_EMAIL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Install Terragrunt
        run: |
          curl -sL "https://github.com/gruntwork-io/terragrunt/releases/download/v${TG_VERSION}/terragrunt_linux_amd64" \
            -o /usr/local/bin/terragrunt
          chmod +x /usr/local/bin/terragrunt
          terragrunt --version

      - name: Terragrunt Apply DEV
        run: |
          cd ${{ env.TG_DIR }}
          terragrunt run-all apply --terragrunt-non-interactive
        env:
          TERRAGRUNT_NON_INTERACTIVE: "true"
          TF_INPUT: "false"

      - name: Configure kubectl
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.REGION }} \
            --project ${{ secrets.GCP_PROJECT }}

      - name: Get GKE Ingress URL
        id: gke
        run: |
          URL=$(kubectl get service sp-logistic-dev-svc -n default \
            -o jsonpath='http://{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
          echo "url=${URL}" >> $GITHUB_OUTPUT

      - name: Package Apigee proxy bundle
        if: steps.gke.outputs.url != ''
        run: |
          sed "s|BACKEND_URL_PLACEHOLDER|${{ steps.gke.outputs.url }}|g" \
            apigee/apiproxy/targets/default.xml > /tmp/default.xml
          cp /tmp/default.xml apigee/apiproxy/targets/default.xml
          cd apigee && zip -r ../apiproxy.zip apiproxy/

      - name: Install apigeecli
        if: steps.gke.outputs.url != ''
        run: |
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH

      - name: Deploy Apigee proxy DEV
        if: steps.gke.outputs.url != ''
        run: |
          TOKEN=$(gcloud auth print-access-token)
          apigeecli apis create bundle \
            --name "${{ env.APIGEE_PROXY }}" \
            --proxy-zip apiproxy.zip \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN"
          apigeecli apis deploy \
            --name "${{ env.APIGEE_PROXY }}" \
            --env "${{ env.APIGEE_ENV }}" \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN" \
            --ovr --wait

  # ==========================================
  # STAGE 3: DEPLOY SIT
  # ==========================================
  deploy-sit:
    needs: security-scans
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'sit'
    runs-on: ubuntu-latest
    environment: sit
    permissions:
      contents: read
      id-token: write
    env:
      TG_DIR: live/sit/gcp
      GKE_CLUSTER: sp-logistic-sit-gke
      APIGEE_ENV: sp-logistic-apigee-env-sit
      APIGEE_PROXY: sp-logistic-sit-gke-proxy
      REGION: asia-southeast1
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SA_EMAIL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Install Terragrunt
        run: |
          curl -sL "https://github.com/gruntwork-io/terragrunt/releases/download/v${TG_VERSION}/terragrunt_linux_amd64" \
            -o /usr/local/bin/terragrunt
          chmod +x /usr/local/bin/terragrunt
          terragrunt --version

      - name: Terragrunt Apply SIT
        run: |
          cd ${{ env.TG_DIR }}
          terragrunt run-all apply --terragrunt-non-interactive
        env:
          TERRAGRUNT_NON_INTERACTIVE: "true"
          TF_INPUT: "false"

      - name: Configure kubectl
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.REGION }} \
            --project ${{ secrets.GCP_PROJECT }}

      - name: Get GKE Ingress URL
        id: gke
        run: |
          URL=$(kubectl get service sp-logistic-sit-svc -n default \
            -o jsonpath='http://{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
          echo "url=${URL}" >> $GITHUB_OUTPUT

      - name: Package Apigee proxy bundle
        if: steps.gke.outputs.url != ''
        run: |
          sed "s|BACKEND_URL_PLACEHOLDER|${{ steps.gke.outputs.url }}|g" \
            apigee/apiproxy/targets/default.xml > /tmp/default.xml
          cp /tmp/default.xml apigee/apiproxy/targets/default.xml
          cd apigee && zip -r ../apiproxy.zip apiproxy/

      - name: Install apigeecli
        if: steps.gke.outputs.url != ''
        run: |
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH

      - name: Deploy Apigee proxy SIT
        if: steps.gke.outputs.url != ''
        run: |
          TOKEN=$(gcloud auth print-access-token)
          apigeecli apis create bundle \
            --name "${{ env.APIGEE_PROXY }}" \
            --proxy-zip apiproxy.zip \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN"
          apigeecli apis deploy \
            --name "${{ env.APIGEE_PROXY }}" \
            --env "${{ env.APIGEE_ENV }}" \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN" \
            --ovr --wait

  # ==========================================
  # STAGE 3: DEPLOY UAT
  # ==========================================
  deploy-uat:
    needs: security-scans
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'uat'
    runs-on: ubuntu-latest
    environment: uat
    permissions:
      contents: read
      id-token: write
    env:
      TG_DIR: live/uat/gcp
      GKE_CLUSTER: sp-logistic-uat-gke
      APIGEE_ENV: sp-logistic-apigee-env-uat
      APIGEE_PROXY: sp-logistic-uat-gke-proxy
      REGION: asia-southeast1
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SA_EMAIL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Install Terragrunt
        run: |
          curl -sL "https://github.com/gruntwork-io/terragrunt/releases/download/v${TG_VERSION}/terragrunt_linux_amd64" \
            -o /usr/local/bin/terragrunt
          chmod +x /usr/local/bin/terragrunt
          terragrunt --version

      - name: Terragrunt Apply UAT
        run: |
          cd ${{ env.TG_DIR }}
          terragrunt run-all apply --terragrunt-non-interactive
        env:
          TERRAGRUNT_NON_INTERACTIVE: "true"
          TF_INPUT: "false"

      - name: Configure kubectl
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.REGION }} \
            --project ${{ secrets.GCP_PROJECT }}

      - name: Get GKE Ingress URL
        id: gke
        run: |
          URL=$(kubectl get service sp-logistic-uat-svc -n default \
            -o jsonpath='http://{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
          echo "url=${URL}" >> $GITHUB_OUTPUT

      - name: Package Apigee proxy bundle
        if: steps.gke.outputs.url != ''
        run: |
          sed "s|BACKEND_URL_PLACEHOLDER|${{ steps.gke.outputs.url }}|g" \
            apigee/apiproxy/targets/default.xml > /tmp/default.xml
          cp /tmp/default.xml apigee/apiproxy/targets/default.xml
          cd apigee && zip -r ../apiproxy.zip apiproxy/

      - name: Install apigeecli
        if: steps.gke.outputs.url != ''
        run: |
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH

      - name: Deploy Apigee proxy UAT
        if: steps.gke.outputs.url != ''
        run: |
          TOKEN=$(gcloud auth print-access-token)
          apigeecli apis create bundle \
            --name "${{ env.APIGEE_PROXY }}" \
            --proxy-zip apiproxy.zip \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN"
          apigeecli apis deploy \
            --name "${{ env.APIGEE_PROXY }}" \
            --env "${{ env.APIGEE_ENV }}" \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN" \
            --ovr --wait

  # ==========================================
  # STAGE 3: DEPLOY PREPROD
  # ==========================================
  deploy-preprod:
    needs: security-scans
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'preprod'
    runs-on: ubuntu-latest
    environment: preprod
    permissions:
      contents: read
      id-token: write
    env:
      TG_DIR: live/preprod/gcp
      GKE_CLUSTER: sp-logistic-preprod-gke
      APIGEE_ENV: sp-logistic-apigee-env-preprod
      APIGEE_PROXY: sp-logistic-preprod-gke-proxy
      REGION: asia-southeast1
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SA_EMAIL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Install Terragrunt
        run: |
          curl -sL "https://github.com/gruntwork-io/terragrunt/releases/download/v${TG_VERSION}/terragrunt_linux_amd64" \
            -o /usr/local/bin/terragrunt
          chmod +x /usr/local/bin/terragrunt
          terragrunt --version

      - name: Terragrunt Apply PREPROD
        run: |
          cd ${{ env.TG_DIR }}
          terragrunt run-all apply --terragrunt-non-interactive
        env:
          TERRAGRUNT_NON_INTERACTIVE: "true"
          TF_INPUT: "false"

      - name: Configure kubectl
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.REGION }} \
            --project ${{ secrets.GCP_PROJECT }}

      - name: Get GKE Ingress URL
        id: gke
        run: |
          URL=$(kubectl get service sp-logistic-preprod-svc -n default \
            -o jsonpath='http://{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
          echo "url=${URL}" >> $GITHUB_OUTPUT

      - name: Package Apigee proxy bundle
        if: steps.gke.outputs.url != ''
        run: |
          sed "s|BACKEND_URL_PLACEHOLDER|${{ steps.gke.outputs.url }}|g" \
            apigee/apiproxy/targets/default.xml > /tmp/default.xml
          cp /tmp/default.xml apigee/apiproxy/targets/default.xml
          cd apigee && zip -r ../apiproxy.zip apiproxy/

      - name: Install apigeecli
        if: steps.gke.outputs.url != ''
        run: |
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH

      - name: Deploy Apigee proxy PREPROD
        if: steps.gke.outputs.url != ''
        run: |
          TOKEN=$(gcloud auth print-access-token)
          apigeecli apis create bundle \
            --name "${{ env.APIGEE_PROXY }}" \
            --proxy-zip apiproxy.zip \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN"
          apigeecli apis deploy \
            --name "${{ env.APIGEE_PROXY }}" \
            --env "${{ env.APIGEE_ENV }}" \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN" \
            --ovr --wait

  # ==========================================
  # STAGE 3: DEPLOY PROD
  # ==========================================
  deploy-prod:
    needs: security-scans
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'prod')
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      id-token: write
    env:
      TG_DIR: live/prod/gcp
      GKE_CLUSTER: sp-logistic-prod-gke
      APIGEE_ENV: sp-logistic-apigee-env-prod
      APIGEE_PROXY: sp-logistic-prod-gke-proxy
      REGION: asia-southeast1
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SA_EMAIL }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Install Terragrunt
        run: |
          curl -sL "https://github.com/gruntwork-io/terragrunt/releases/download/v${TG_VERSION}/terragrunt_linux_amd64" \
            -o /usr/local/bin/terragrunt
          chmod +x /usr/local/bin/terragrunt
          terragrunt --version

      - name: Terragrunt Apply PROD
        run: |
          cd ${{ env.TG_DIR }}
          terragrunt run-all apply --terragrunt-non-interactive
        env:
          TERRAGRUNT_NON_INTERACTIVE: "true"
          TF_INPUT: "false"

      - name: Configure kubectl
        run: |
          gcloud container clusters get-credentials ${{ env.GKE_CLUSTER }} \
            --region ${{ env.REGION }} \
            --project ${{ secrets.GCP_PROJECT }}

      - name: Get GKE Ingress URL
        id: gke
        run: |
          URL=$(kubectl get service sp-logistic-prod-svc -n default \
            -o jsonpath='http://{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
          echo "url=${URL}" >> $GITHUB_OUTPUT

      - name: Package Apigee proxy bundle
        if: steps.gke.outputs.url != ''
        run: |
          sed "s|BACKEND_URL_PLACEHOLDER|${{ steps.gke.outputs.url }}|g" \
            apigee/apiproxy/targets/default.xml > /tmp/default.xml
          cp /tmp/default.xml apigee/apiproxy/targets/default.xml
          cd apigee && zip -r ../apiproxy.zip apiproxy/

      - name: Install apigeecli
        if: steps.gke.outputs.url != ''
        run: |
          curl -L https://raw.githubusercontent.com/apigee/apigeecli/main/downloadLatest.sh | sh
          echo "$HOME/.apigeecli/bin" >> $GITHUB_PATH

      - name: Deploy Apigee proxy PROD
        if: steps.gke.outputs.url != ''
        run: |
          TOKEN=$(gcloud auth print-access-token)
          apigeecli apis create bundle \
            --name "${{ env.APIGEE_PROXY }}" \
            --proxy-zip apiproxy.zip \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN"
          apigeecli apis deploy \
            --name "${{ env.APIGEE_PROXY }}" \
            --env "${{ env.APIGEE_ENV }}" \
            --org ${{ secrets.GCP_PROJECT }} \
            --token "$TOKEN" \
            --ovr --wait

  # ==========================================
  # STAGE 4: NOTIFY
  # ==========================================
  notify:
    if: always()
    needs: [deploy-dev, deploy-sit, deploy-uat, deploy-preprod, deploy-prod]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mocking GChat Webhook"
